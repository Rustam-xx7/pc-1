# Use of Next.js

### we can use app basse routing .
- we have to just make folder , and use it as routing . 

# Server Components in Next.js

### In Next js every thing is sever component 
-  next js a sever component ar client component alada . useState ba useEffect ai sob use korte gale amader first a page.js ke "use client" banate hobe . just write 
``` 
"use client"
```

- server side jeta dev dekhbe , clint side jeta clint side consol a dekha jabe . 
- Bydefault server side code hoy next.js a . mane we can read server side things in the server side console .

- we can make  only a particular component **clicnt component**  , and use it in our **server component** page.js .